import React from 'react'
import { useState } from "react";


    const onClick = () => setIsHovered(true);
    // On change l’état de isHovered à false
    
   
    className = isClick ? "Yes" : "No";
  return (

    <div onClick={onClick}>Hello world</div>
    
  )
}

export default Button2